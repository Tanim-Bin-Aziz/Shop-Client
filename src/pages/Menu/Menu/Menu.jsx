import { Helmet } from 'react-helmet-async';

const Menu = () => {
    return (
        <div>
            <Helmet>
                <title>Shop | Menu</title>
            </Helmet>
        </div>
    );
};

export default Menu;